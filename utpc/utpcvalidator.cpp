#include <bits/stdc++.h>
#include "testlib.h"
 
using namespace std;
 
int main(int argc, char* argv[]) {
    registerValidation(argc, argv);
    long long m = (long long)1e9;
    long long n = inf.readLong(2, m, "a");
    inf.readSpace();
    inf.readInt(2, m, "b");
    inf.readSpace();
    inf.readInt(1, m, "x");
    inf.readSpace();
    inf.readInt(1, m, "x");
    inf.readEoln();
    inf.readEof();
}